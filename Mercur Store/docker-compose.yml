version: "3.9"
services:
  db:
    image: postgres
    volumes:
      - postgres-db:/var/lib/postgresql
    environment:
      - POSTGRES_DB=mercur_db
      - POSTGRES_USER=mercur_store_db
      - POSTGRES_PASSWORD=mercur
    ports:
      - 5432:5432
volumes:
  postgres-db: